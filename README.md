# vim-royale
* Because Nano sucks.
* Because Emacs sucks.
* Because Rust sucks.

**May the best man *vim*.**

# Setup
`npm install`

## Start the server
`ts-node src/server/index.ts`

## Start the client
`ts-node src/index.ts 2> out`
