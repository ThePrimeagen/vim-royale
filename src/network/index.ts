import Player from "../objects/player";
import { Command } from "../types";
import BufferWriter from "../util/buffer-writer";

export function createPositionUpdate(player: Player, commands: Command[]): BufferWriter {
}


