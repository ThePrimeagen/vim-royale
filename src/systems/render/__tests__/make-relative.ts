import makeRelative from '../make-relative';

describe("makeRelative", function() {

    it("move the point relative to the top left", function() {
        const x = 50;
        const y = 50;
        const leftX = 30;
        const leftY = 45;

        //expect(makeRelative(leftX, leftY, x, y)).toEqual([ 20, 5 ]);
    });
});



