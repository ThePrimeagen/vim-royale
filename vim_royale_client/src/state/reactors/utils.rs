use encoding::server::{PlayerStart, self};
use vim_royale_view::message::Msg;

/*
pub fn to_player_start(msg: &Msg) -> Option<&PlayerStart> {
    if let Msg::Message(msg) = msg {
        if let server::Message::PlayerStart(m) = &msg.msg {
            return Some(m);
        }
        return None;
    }
    return None;
}


*/
